vẽ full fragment if else về case trả về gì, mô tả chi tiết luồng song song, đơn luồng, chi tiết flow hoạt đông logic và check logic để sửa lại logic nếu sai, bô sung logic, service nếu cần, nâng cấp dễ maintain, scale, reuse, kiến trúc tối ưu nâng cao, vẽ tách riêng từng sequence chi tiết , tất cả sequence phải có fragment logic sẽ trả về gì, kiểm tra cần bổ sung thêm api gì và logic cần chỉnh gì không, thêm flow check auth, permission trong code django nếu có 




bổ sung hình DB thô, không cần ghi code sql, bổ sung tầng người dần tương sau đó mới gọi tới controller, kham khảo @user_seq.plantuml 


chỉnh sửa authen, permission cho đúng người dùng cho từng chức năng, thêm chú thích  admin, staff vì đây là hệ thống django, viết thêm chức năng nếu cần hoặc còn thiếu, chú thích rõ check_authentication với role nào, thêm note đầy đủ chung không riêng cho bất kì ngôn ngữ nào, framework nào cả


thiết kế tối ưu db theo phong cách microservervice nên không đặt constraint quá để khỏ
sau này tách db, chú thích những hằng số quy định kế bên